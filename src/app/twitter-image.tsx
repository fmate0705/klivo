// A Twitter/X kártya ugyanazt a generált képet használja, mint az OG.
import Image, { alt, size, contentType } from "./opengraph-image";

export const dynamic = "force-static";
export { alt, size, contentType };
export default Image;
